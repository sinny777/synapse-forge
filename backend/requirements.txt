# ToolRouter Requirements
# Python 3.10+

# Core ML/AI Libraries
torch>=2.0.0
sentence-transformers>=2.2.0
numpy>=1.24.0
datasets>=2.14.0
accelerate>=1.1.0

# Vector Stores
faiss-cpu>=1.7.4  # Use faiss-gpu for GPU support
chromadb>=0.4.0

# Hybrid Retrieval (Enhancement 2)
rank-bm25>=0.2.2

# LLM Integration
litellm>=1.0.0

# Agent Frameworks (for Real Mode)
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.0.5
langgraph>=0.0.20
beeai-framework>=0.1.0  # Optional: for BeeAI scenarios

# MCP Protocol
mcp>=0.9.0
fastmcp>=0.1.0  # For FastMCP server support

# Data Processing
tqdm>=4.65.0

# Utilities
python-dotenv>=1.0.0

# Database & Storage (MongoDB + Redis + Milvus)
motor>=3.6.0             # Async MongoDB driver
pymongo>=4.9.0           # MongoDB client utilities
pymilvus>=2.5.0          # Milvus vector database client
redis>=5.0.0             # redis.asyncio for caching & checkpoints
boto3>=1.26.0
ibm-cos-sdk>=2.13.0

# Docker Control Plane (Phase 3)
docker>=7.0.0            # Docker SDK for workspace container orchestration

# Optional: For development
pytest>=7.4.0
black>=23.0.0
mypy>=1.5.0
fastapi
uvicorn
pydantic
authlib>=1.3.0
itsdangerous>=2.1.2
httpx>=0.25.0
